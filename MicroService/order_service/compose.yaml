services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: orders
    ports:
      - "3308:3306"
    volumes:
      - order_mysql:/var/lib/mysql

volumes:
  order_mysql:

